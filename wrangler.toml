# Cloudflare Worker config for the Yahoo Finance relay (worker.js).
# Deploy with:  wrangler deploy
# Then copy the printed https://<name>.<subdomain>.workers.dev URL into the
# WORKER_URL constant in index.html.
name = "cgt-yahoo-relay"
main = "worker.js"
compatibility_date = "2024-11-01"
